{
  "name": "@paintcannon/release",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run check",
    "test": "npm run check",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
